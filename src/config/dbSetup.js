// const connection = require('./db'); // Import the db connection from db.js

// const query = `
//   ALTER TABLE users
//   ADD CONSTRAINT unique_email UNIQUE (email);
// `;

// connection.query(query, (err, results) => {
//     if (err) {
//         console.error('Error adding unique constraint to email field:', err);
//     } else {
//         console.log('Unique constraint added to email field successfully.');
//     }
//     connection.end(); // Close the connection after the query
// });
